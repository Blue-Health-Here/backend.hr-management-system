// Enums
export enum DesignationStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}

export enum LevelHierarchy {
    ENTRY = 'entry',
    MID = 'mid',
    SENIOR = 'senior',
    LEAD = 'lead',
    MANAGER = 'manager',
    DIRECTOR = 'director'
}
